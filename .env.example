# =================================================================
# Bot de Reservas de Lavandería - Variables de Entorno
# =================================================================
# 
# INSTRUCCIONES DE SEGURIDAD:
# 1. NUNCA comitear archivo .env a Git (ya está en .gitignore)
# 2. Para desarrollo local: Copiar a .env y llenar con valores reales
# 3. Para Railway/Production: Agregar variables en dashboard de Railway
# 4. Para Docker: Pasar como variables de entorno o en archivo .env
#

# -------- WHATSAPP CONFIGURATION --------
# Tu número de teléfono (para pruebas privadas)
# Formato: código_país + número sin símbolos (ej: 56965849477)
OWNER_PHONE=56965849477

# Nombre exacto del grupo de WhatsApp donde reacciona el bot
ALLOWED_GROUP_NAME=botTest

# Mención para activar el bot en grupos
BOT_MENTION=@bot

# -------- GOOGLE SHEETS CONFIGURATION --------
# ID de tu Google Sheet (en la URL: docs.google.com/spreadsheets/d/{ID}/edit)
GOOGLE_SHEETS_ID=1Rx4uRjqhD4Vqu9BGyBjtcMHHTCQ_JMl_L3XdH8lswTE

# Email de la cuenta de servicio (del JSON de credenciales)
GOOGLE_SERVICE_ACCOUNT_EMAIL=residencia167@botagenda-478614.iam.gserviceaccount.com

# -------- GOOGLE SERVICE ACCOUNT CREDENTIALS --------
# OPCIÓN 1: Para Railway/Docker/Producción
# Pegar el JSON completo de credenciales como STRING (sin saltos de línea)
# Abre tu archivo JSON y copia TODO el contenido (desde { hasta })
GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"botagenda-478614","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}

# OPCIÓN 2: Para desarrollo local
# Descomenta esta línea y usa ruta al archivo JSON
# GOOGLE_SERVICE_ACCOUNT_JSON=./botagenda-478614-e2daa61b626a.json

# -------- APP CONFIGURATION --------
# Zona horaria de tu región
TIMEZONE=America/Argentina/Buenos_Aires

# Entorno (development o production)
NODE_ENV=production

# Nivel de logs (debug, info, warn, error)
LOG_LEVEL=info


# Ruta local al archivo JSON de credenciales (alternativa a variables individuales)
# Dejar vacío si usas GOOGLE_PRIVATE_KEY directamente
GOOGLE_SERVICE_ACCOUNT_JSON=./credentials.json

# -------- ENVIRONMENT --------
# Desarrollo o producción
NODE_ENV=development

# -------- BOT CONFIGURATION (Opcional) --------
# Mención del bot (defecto: @bot)
BOT_MENTION=@bot

# Zona horaria (defecto: America/Mexico_City)
TIMEZONE=America/Mexico_City

# Timeout para respuestas de Sheets (ms)
RESPONSE_TIMEOUT=5000

# -------- LOGGING (Opcional) --------
# Nivel de logs: debug, info, warn, error
LOG_LEVEL=info

# -------- PUPPET DEBUGGING (Opcional) --------
# Para debugging de whatsapp-web.js (descomenta para ver logs detallados)
# DEBUG=whatsapp-web.js:*
